# lefthook.yml
# https://github.com/evilmartians/lefthook

pre-commit:
  parallel: false
  commands:
    format:
      run: bunx biome format --write .
    lint:
      run: bunx biome check .

pre-push:
  parallel: true
  commands:
    lint:
      run: bunx biome check .
